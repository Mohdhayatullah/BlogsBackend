package org.blogs.Blogs.service;import lombok.RequiredArgsConstructor;import org.blogs.Blogs.entity.BlogPost;import org.blogs.Blogs.entity.Follow;import org.blogs.Blogs.entity.UserEntity;import org.blogs.Blogs.repository.BlogRepo;import org.blogs.Blogs.repository.FollowRepository;import org.blogs.Blogs.repository.UserRepository;import org.springframework.stereotype.Service;import java.util.List;@Service@RequiredArgsConstructorpublic class FeedService {    private final FollowRepository followRepository;    private final BlogRepo blogRepository;    private final UserRepository userRepository;    public List<BlogPost> getFeed(Long userId) {        UserEntity user = userRepository.findById(userId)                .orElseThrow(() -> new RuntimeException("User not found"));        // 👥 get following users        List<Follow> follows = followRepository.findByFollower(user);        List<UserEntity> followingUsers = follows.stream()                .map(Follow::getFollowing)                .toList();        // 📰 fetch blogs        return blogRepository.findByUserInOrderByCreatedAtDesc(followingUsers);    }}