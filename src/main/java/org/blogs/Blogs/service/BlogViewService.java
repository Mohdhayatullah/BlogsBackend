package org.blogs.Blogs.service;import lombok.RequiredArgsConstructor;import org.blogs.Blogs.entity.BlogPost;import org.blogs.Blogs.entity.BlogView;import org.blogs.Blogs.entity.UserEntity;import org.blogs.Blogs.repository.BlogRepo;import org.blogs.Blogs.repository.BlogViewRepository;import org.blogs.Blogs.repository.UserRepository;import org.springframework.stereotype.Service;@Service@RequiredArgsConstructorpublic class BlogViewService {    private final BlogViewRepository blogViewRepository;    private final BlogRepo blogRepository;    private final UserRepository userRepository;//    public void addView(Long blogId, Long userId, String ipAddress) {////        BlogPost blog = blogRepository.findById(blogId)//                .orElseThrow(() -> new RuntimeException("Blog not found"));////        UserEntity user = null;////        if (userId != null) {//            user = userRepository.findById(userId)//                    .orElseThrow(() -> new RuntimeException("User not found"));////            // ✅ Prevent duplicate user view//            if (blogViewRepository.existsByBlogAndUser(blog, user)) {//                return;//            }//        } else {//            // ✅ Prevent duplicate guest view//            if (blogViewRepository.existsByBlogAndIpAddress(blog, ipAddress)) {//                return;//            }//        }////        BlogView view = BlogView.builder()//                .blog(blog)//                .user(user)//                .ipAddress(ipAddress)//                .build();////        blogViewRepository.save(view);////        // ✅ update fast counter//        blog.setViewCount(blog.getViewCount() + 1);//        blogRepository.save(blog);//    }    public void addView(Long blogId, Long userId, String ipAddress) {        BlogPost blog = blogRepository.findById(blogId)                .orElseThrow(() -> new RuntimeException("Blog not found"));        // check duplicate logic...        UserEntity user = null;        if (userId != null) {            user = userRepository.findById(userId)                    .orElseThrow(() -> new RuntimeException("User not found"));            // ✅ Prevent duplicate user view            if (blogViewRepository.existsByBlogAndUser(blog, user)) {                return;            }        } else {            // ✅ Prevent duplicate guest view            if (blogViewRepository.existsByBlogAndIpAddress(blog, ipAddress)) {                return;            }        }        BlogView view = BlogView.builder()                .blog(blog)                .user(user)                .ipAddress(ipAddress)                .build();        blogViewRepository.save(view);        // ✅ optimized update        blogRepository.incrementViewCount(blogId);    }}